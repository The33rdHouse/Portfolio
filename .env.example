# Public placeholder configuration only.
# Do not put real secrets, production keys, passwords, tokens, or private URLs in this file.

APP_NAME="The 33rd House Portfolio"
APP_ENV="development"
PUBLIC_SITE_URL="https://example.com"

# Example placeholders only
PUBLIC_ANALYTICS_ID="replace-with-public-id-if-required"
API_BASE_URL="https://api.example.com"

# Keep real values in your deployment provider's secret manager.
